# Integrates (0,inf) over multivariate normal

Integrates (0,inf) over multivariate normal

## Usage

``` r
zeroToInfNormCDF(mu, sigma)
```

## Arguments

- mu:

  vector of mean parameters

- sigma:

  covariance matrix

## Value

double
